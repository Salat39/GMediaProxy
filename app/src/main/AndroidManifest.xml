<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="GMediaProxy"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GMediaProxy.Base">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LogActivity"
            android:exported="true"
            android:theme="@style/Theme.GMediaProxy.Base" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".OnlineMusicService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.geely.service.ONLINE_MUSIC" />
            </intent-filter>
        </service>

        <service
            android:name=".MediaSessionNotificationListenerService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".OnlineMusicUpdateReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.salat.gmproxy.MEDIA_UPDATE" />
                <action android:name="com.salat.gmproxy.ENABLE" />
                <action android:name="com.salat.gmproxy.DISABLE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
